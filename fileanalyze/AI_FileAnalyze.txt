Generate Python Framework code that reads a csv or a pipe delimited (delimiter is a parameter) file($Filename) whose Metadata in stored in ${Filename}_Meta...The idea is to generate useful insights on the file as much as possible and provide key summaries to the users.

The Metadata is stored as a pipe delimited PrimaryKey followed bt 5 value info-pair for every field values in the $Filename.
FileKey-comma delimited Primary Key Columns
FieldName-Name of the Column
FieldType-Char/Num/Date/Datetime
FieldLength-format text/float/decimal precision
FieldDesc-Description of the Field
FieldDType-Should be used as a Dimension(D) or a Measure(M)
The Python code generates descriptive statistics on all the key fields

Rules 
-Provides a HTML based UI to point to the location of the File, option to select the delimiter etc and hits Submit button to proceed to the next screen
-On Submit, The framework should parallely analyze the file and create quick statistics such as Char Fields[Frequencies(N)], Num fields [Min,Max,Sum,Mean,Median] and Date Fields for [Min,Max]

-Loads a Two Tab HTML based UI Layout
-Tab 1
	-Reads the csv or a pipe delimited file and meta file and loads the Field Names into a Tableau Dashboard style Menu. Show the Quick Stats on Hovering the Field Names
	-The user drags and Drops the Dimensions and Measure Fields to analyze the file...
	-The user specifies which type of chart they want to see from a list of Visualizations like Line, Pie,Bar,Stacked Bar,Histogram and Scatter Plots.
	-On Submit button click, the python code creates descriptive statistics on that file based on the fields selected with highest quality Charts, Plots and graphs.
	
-Tab 2
	-Two Panel Layout
		Panel 1 - width 100%, Height 10%
			-Reads the csv or a pipe delimited file and meta file and loads the Field Names into Live Search Box for all Fields. For Dimension type fields the search box filters by the text, For Measure type fields the search box uses expressions like between value1 and Value2
		Panel 2 - width 100%, Height 90%
			-Reads the csv or a pipe delimited file and meta file and loads the File into a table grid style Frame with filters from Panel 1 applied
			-Grid should have inbuilt line numbers

Suggest any other improvements to the process.
The framework should be designed that many users could run the code simultaneously and independently...so there should not be any file locking or overwriting each others files/outputs or customizations etc.
The Python Framework designed should be able to executed on a Windows machine as well as on a Unix Server...Make sure the code can handle multiple environments.
All environment-driven parameters should be defined in ../venv/.env file

In the Data Grid, Use Live Search Boxes with data assisted text boxes for the Dimension Filter, prepopulate the "Dimension Text filter" with sorted Unique values for that Dimension from the file
upgrade the Measure filter to a live, data-assisted searchable dropdown and auto-load it with 10 value ranges (develop 10 equal ranges from Min to Max) from the selected measure column in the current file